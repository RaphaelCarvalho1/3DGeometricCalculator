import { Camera } from './camera';
import { Mesh } from './mesh';

class Scene {
    private _meshes: Mesh[];
    private _cam: Camera;

    public appendMesh(mesh: Mesh): void {

    }

    public draw(): void {

    }
}